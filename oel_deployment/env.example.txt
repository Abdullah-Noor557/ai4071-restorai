# =============================================================================
# .env.example - copy to .env and fill in your real keys.
#
# IMPORTANT: .env is git-ignored AND docker-ignored. Only the running
# container ever sees these values, and they are NEVER copied into the
# image at build time.
# =============================================================================

# Required - OpenAI key (chat model + embeddings)
OPENAI_API_KEY=sk-replace-me

# Optional - enables real Gemini vision analysis. Without it the agent
# returns a polite "vision unavailable" response.
GOOGLE_API_KEY=

# Optional - bind addresses (defaults shown)
# API_HOST=0.0.0.0
# API_PORT=8000
